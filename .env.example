# Postgres (Neon free tier)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Auth — secreto para firmar JWT de sesion (genera con: openssl rand -base64 32)
AUTH_SECRET="cambia-esto-por-un-secreto-largo-aleatorio"

# Anthropic — tutor IA y explicaciones
ANTHROPIC_API_KEY="sk-ant-..."

# Voyage AI — embeddings para el dedup semantico (opcional; sin esto, dedup lexico)
VOYAGE_API_KEY="pa-..."

# Stripe — suscripcion premium
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRICE_PREMIUM_MENSUAL="price_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Resend — email de alertas de convocatoria (opcional; sin esto no se envia)
RESEND_API_KEY="re_..."
EMAIL_FROM="TestSAS <avisos@tudominio.es>"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
